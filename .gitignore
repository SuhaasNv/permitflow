# Python
__pycache__/
*.pyc
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage

# Node
node_modules/
dist/
dist-ssr/
*.local
playwright-report/
test-results/

# Env / secrets
.env
.env.*
!.env.example

# Uploads / data
backend/data/
uploads/

# OS / editor
.DS_Store
.idea/
.vscode/
*.log

# Assessment brief and other office docs never enter the repo (the decks under docs/14-debrief/ are the exception)
*.docx
*.pptx
*.xlsx
!docs/14-debrief/**/*.pptx

# Runtime data, partial uploads, coverage and generated prototype output
data/
*.part
coverage/
playwright/.cache/
docs/04-design/prototype-src/project/
docs/04-design/prototype-src/static/
docs/04-design/prototype-src/shots/

# Personal study notes (not part of the deliverable)
notes/

# vitest reporter output
frontend/.vitest/
# Copied in by CI for the image build (US-094); the source is the root RELEASE_NOTES.md
frontend/RELEASE_NOTES.md

# Presenter copies of the decks (with speaker notes) stay local; the shared copies carry no notes
*-presenter.pptx
.playwright-mcp/
