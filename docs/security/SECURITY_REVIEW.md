# Security review: the owner's hardening checklist and the four abuse scenarios

Story US-058, 19 September 2026. The owner's brief was a twelve-item hardening checklist followed by four concrete attacks ("how I would ruin your life"): hammer the sign-in endpoint until it falls over, fill the database with garbage sign-ups, hit the most expensive endpoint until the API bill explodes, and scrape everything unnoticed. This document takes each item and each attack, says what the code did before this story, what it does now, and where the evidence is. It complements the threat model (`THREAT_MODEL.md`, T1 to T22), which is organised by threat; this one is organised by the owner's questions.

Reviewed by an engineer against the code, with the tools named in each row. It is not a penetration test.

## The checklist, item by item

| # | Asked for | Before US-058 | Now | Evidence |
|---|-----------|---------------|-----|----------|
| 1 | Hide my API keys | Keys only in environment variables; `.env` ignored by git; runtime keys on Railway, CI keys as GitHub secrets; nothing in the image | Unchanged. gitleaks over the full history (111 commits at the time): no leaks. The LangSmith key moved from the clipboard into GitHub, Railway and `.env` through shell commands, never through the AI | `.gitignore`, `ci.yml` secret scan job, `docs/operations/OPERATIONS.md` "Secrets and variables" |
| 2 | Check my environment variables and protect my admin routes | Every variable documented with its default; the app refuses to start without `JWT_SECRET` outside tests; `TRUSTED_PROXIES` controls whose `X-Forwarded-For` is believed. Admin: a role and a frontend route guard exist; the admin API (US-070 to US-073) is not built, so there is nothing to protect yet beyond the guard | Five variables added and documented (`RATE_LIMIT_PER_MINUTE`, `LOGIN_ATTEMPTS_PER_MINUTE`, `MAX_DRAFTS_PER_USER`, `AI_RUNS_PER_USER_PER_DAY`, `AI_RUNS_PER_DAY`) | `app/core/settings.py` (`validate_for_startup`), `.env.example`, `OPERATIONS.md`; `app/api/deps.py` (`require_role`) |
| 3 | Add proper authentication and make sure users only access what they should | Argon2 password hashes; signed JWT (8 h) with the role claim re-checked against the user row on every request; every route declares its roles; ownership enforced as 404 (no existence leak); every endpoint has an authorization test (wrong role, wrong owner, wrong state) | Unchanged | `app/core/security.py`, `app/api/deps.py`, `tests/integration/*` (authorization cases per router), `THREAT_MODEL.md` T2, T3, T15 |
| 4 | Sanitize my forms and protect against XSS | Pydantic validation with max lengths and patterns on every field; React escapes all output and the codebase has no `innerHTML`; uploaded filenames sanitised and served as `attachment` with `nosniff`; no user content is ever rendered as HTML | Content Security Policy on both tiers: the frontend admits scripts only from itself and connections only to its one API origin; the API declares `default-src 'none'`. Verified by driving the built bundle through sign-in, application, upload and list under the exact policy: zero violations | `app/schemas/*`, `frontend/docker/security-headers.conf.template`, `app/main.py` `security_headers`; T11 |
| 5 | Add rate limiting and secure my API endpoints | One limiter, failed sign-ins only (10 per minute per client) | Every request is counted per client in a sliding minute (240), sign-in attempts of any outcome separately (20, because Argon2 makes each attempt cost CPU); 429 with `Retry-After` in the standard error envelope; health endpoints exempt; the client key honours `X-Forwarded-For` only behind a trusted proxy, so an attacker cannot pick a fresh bucket per request | `app/core/rate_limit.py`, `app/main.py` `rate_limit` middleware; `tests/unit/test_rate_limit.py`, `tests/integration/test_abuse_limits.py` |
| 6 | Check my CORS settings | Explicit allowlist per environment (`CORS_ORIGINS`), no wildcard, `allow_credentials=False`, methods and headers enumerated; the production list is the two production hosts | Unchanged; the CSP `connect-src` now mirrors it from the frontend side | `app/main.py`, Railway variables per environment |
| 7 | Add security headers | API: `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Cache-Control: no-store`. Frontend: the first three | API adds `Content-Security-Policy: default-src 'none'; frame-ancestors 'none'` (docs pages excepted outside production), `Strict-Transport-Security`, `Permissions-Policy`, `Cross-Origin-Opener-Policy`. Frontend adds CSP (one API origin rendered from `API_URL` at container start), HSTS, `Permissions-Policy` | `app/main.py`; `frontend/docker/security-headers.conf.template`, `entrypoint.sh`; `tests/integration/test_abuse_limits.py::test_api_answers_carry_the_hardening_headers`; the image was built and its headers curled |
| 8 | Turn off debug mode | Interactive docs and the OpenAPI schema are served only outside production; uvicorn runs without reload in the image; FastAPI debug is off; unhandled errors return a fixed message and a request id, never a traceback | Unchanged | `app/main.py` (`expose_docs`), `backend/Dockerfile`, `catch_unhandled` |
| 9 | Update my dependencies and remove anything I don't need | `pip-audit` and `npm audit` ran in CI but were allowed to fail, and `pip-audit` had been failing since it was added (it choked on the project's own editable line), so the job proved nothing. Lockfiles pinned | `pip-audit` fixed (`--no-emit-project`) and **blocking**; `bandit` added, blocking at medium severity (three type-narrowing `assert`s replaced by explicit raises so the scan is clean); `npm audit` blocking at high; the image job now waits for the audit. Results on 19 Sep: 0 known vulnerabilities in either tree, 0 bandit findings. Nothing removed: every runtime dependency is imported (the `xxhash` and `zstandard` wheels belong to LangSmith) | `ci.yml` job "Dependency and code audit" |
| 10 | Check for exposed files | nginx serves only the built bundle, `config.js`, fonts and the favicon; uploads live on a volume outside any static path and are served only through an authorised endpoint with server-generated keys; no source maps in the production build; `.env` never in the image | Unchanged; verified by listing the built image's document root (index.html, one JS and one CSS bundle, config.js, favicon, fonts, nginx's 50x page) | `frontend/docker/nginx.conf.template`, `app/infra/storage.py`, T5, T15 |
| 11 | Secure my database and hash passwords properly | Argon2id via `argon2-cffi`; parametrised SQLAlchemy everywhere (no string SQL with user input); the database is reachable only on Railway's private network; per-environment `JWT_SECRET`; migrations forward-only | Unchanged. Known gap kept honest: the database role is the platform default, not a least-privilege role, and there is no encryption beyond the platform's disk encryption | `app/core/security.py`, T10, T7 |
| 12 | Check my git history for leaked secrets and run a full security audit | gitleaks in CI on every push over the full history | Run again locally over 111 commits: no leaks. Full audit = this document plus the threat model (T1 to T22), bandit, pip-audit, npm audit, and the abuse tests below | `ci.yml` secret scan; this file |

## The four attacks, and what happens now

| # | The attack | Before | Now | Test |
|---|------------|--------|-----|------|
| 1 | "Log in and hit your backend 10,000 times a minute until the login page crashes" | Only failed sign-ins were limited; 10,000 correct-password requests a minute would each run Argon2 (about 50 ms of CPU) and win | The sign-in bucket admits 20 attempts per client per minute, any outcome; the 21st is a 429 with `Retry-After` and costs no hashing. Every other endpoint sits under the general 240 per minute. Both limits are per process, so a distributed attack needs the edge limit named below | `test_sign_in_attempts_of_any_outcome_are_limited_per_client` |
| 2 | "Spam your signup until the database is full of garbage" | There is no sign-up: accounts are created by seeding (and, once US-073 is built, by an admin). The equivalent was unlimited draft applications per operator, each with up to four 10 MB files | An operator may hold at most 20 open drafts (`MAX_DRAFTS_PER_USER`); the 21st is a 409 that says so and how to free a slot. Storage per operator is therefore bounded at 20 × 4 × 10 MB | `test_an_operator_may_hold_a_bounded_number_of_drafts` |
| 3 | "Hit your most expensive endpoint until the API bill is $950,000" | Every upload and every re-run was a model call (up to 20,000 characters); the only brake was "one run at a time per document" | Verification runs are counted in the database over a rolling day, per applicant (60) and per platform (1,000, the cost ceiling). Over quota, the run is stored as `unavailable` with the reason `daily_limit_reached`, nothing is sent to the model, the document stays, the application can still be submitted, and the officer sees why. Counting in the database means the ceiling survives restarts and applies across workers. At `gpt-4.1-mini` prices the platform ceiling is under two dollars a day | `test_verification_runs_over_the_daily_quota_are_stored_unavailable` |
| 4 | "Scrape all your data so that you don't even notice" | Every endpoint behind authentication; operators see only their own applications; officers see all, which is their job | The general limiter caps any one client at 240 requests a minute; every request is logged with client, path, status and timing; every read of an application by an officer is a request-log line and every action an audit event. A stolen officer token remains the real scraping risk, which is why sessions expire in eight hours and the role is re-checked on every request | `test_every_request_counts_against_the_general_bucket_but_health_does_not` |

## What production would add

- Rate limiting at the edge (Cloudflare or Railway's proxy rules) and the same windows in Redis, so the limit holds across processes and regions. The in-process limiter is the MVP simplification recorded in `SCOPE.md` since Sprint 1.
- A least-privilege database role and a retention schedule (SEC-012).
- `httpOnly` cookie sessions in place of `sessionStorage` (T14), which the CSP now makes a smaller risk but does not remove.
- A penetration test against the deployed environment; this review is source-level.

## Verification record

- Backend: 747 tests green on a real PostgreSQL, including the 8 new abuse tests; `ruff`, `mypy --strict`, `bandit` clean; `pip-audit` clean.
- Frontend: the image built locally; `curl -I` shows the rendered headers with the right API origin; the built bundle driven by Playwright under the exact CSP through landing, sign-in, new application, document upload with a check, and the applications list, with zero CSP violations and zero console errors; the licence preview's `<object data="blob:">` confirmed allowed by `object-src blob:`.
- gitleaks over the full history: no leaks.
