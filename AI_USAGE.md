# AI Usage

How AI tools were used to build PermitFlow, what they were given, how their output was checked, and where they were wrong. The short version is in `README.md`; this is the full record. The AI inside the product (the document verifier) is a separate topic, covered in `docs/07-ai/AI_VERIFICATION_DESIGN.md` and `docs/07-ai/AI_EVALUATION.md`.

## 1. Tools and what each did

| Tool | Used for |
|------|----------|
| Claude Code (Anthropic's terminal coding agent), models Claude Opus 5 for most sessions and Claude Fable 5.1 for some | The pair for the whole build: solutioning documents, design system and prototype, every story's code and tests, docs, commit messages, the sprint rituals. It worked under two standing instruction files (below) and never pushed to GitHub without an explicit yes in that turn. |
| Cursor | The editor. Claude Code ran in its integrated terminal; Cursor was used to read diffs before every commit, to navigate the code the agent had written, and for the small edits that were quicker by hand than by prompt. Its own AI features were not used to generate code, so that one tool's output was under review, not two. |
| Wispr Flow | Voice dictation for the long prompts: the briefs for stories, reviews and the two checklists were spoken, then edited before being sent. The prompts recorded in section 4 are the edited form. |
| Claude Code subagents | Independent reviews run in parallel with separate briefs, each briefed as read-only (one wrote a scratch test file that was swept into a commit; section 6) and each required to reproduce a finding before reporting it: two design-critique passes on the prototype (design phase), three devil's-advocate edge-case reviews (Sprint 2, `docs/11-reviews/EDGE_CASE_REVIEW.md`), a layout audit at five widths (`docs/11-reviews/LAYOUT_AUDIT.md`), three bug hunts (backend rules, frontend interaction, seams; `docs/11-reviews/BUG_HUNT_REVIEW.md`), a logic audit of the Document checks counters, an independent review of the whole submission against the brief, a final bug hunt on the last day's changes, and a research pass on current AI-assurance tooling (promptfoo, LangSmith, Langfuse, OpenTelemetry GenAI, Project Moonshot). Fixes never came from a reviewer; they went through the normal story flow with tests. |
| Claude in Chrome | Three persona run-throughs in a real browser with the demo PDFs (operator, officer, operator again; the third on the deployed development environment as the Sprint 3 acceptance), the DNS and Railway dashboard steps the CLI could not do (registrar records, custom domains, project tokens), the LangSmith setup (API key creation, checking traces and experiments), and layout checks at phone, tablet and desktop widths. I signed in myself every time; secrets moved from the clipboard into GitHub, Railway and `.env` through shell commands, so the agent never read a credential. |
| Notion, through its MCP server connected to Claude Code | The story board ("PermitFlow, Xtremax Assessment"): five epics (E0 foundation and delivery, UC1, UC2, UC3 deferred, E4 admin) and stories US-000 to US-073 with status, priority, sprint day and notes; status moved as stories started and finished, notes with branch names and follow-ups; kept 1:1 with `docs/05-planning/USER_STORIES.md`. The board was the live truth for planning, the markdown the record in the repository. |
| A second Claude Code session, in parallel with the release, for the debrief material (US-059) | The pitch deck, the technical deck and three videos, made after the code was frozen with Claude Code video skills; the full account is the appendix at the end of this file. |
| Cloudflare `security-audit` skill (24 Sep 2026) | An open-source Claude Code skill from Cloudflare (`github.com/cloudflare/security-audit-skill`, installed at user level after its files were read: guidance documents plus two local report validators with no network or shell calls). It drives a full source-first audit: reconnaissance agents, coverage-led hunter waves over a deterministic coverage ledger, coverage critics, independent verifiers, and schema-validated `findings.json` with `confirmed`, `needs_validation` and `rejected` records. Run on `dev` at `ee67688`, standard profile, 15-agent budget, source-only (no target code executed); its output lives outside the repository in `~/security-audit-skill/permitflow/run-1/`, and fixes, if any, go through the normal story flow. |
| Railway MCP and CLI, GitHub CLI | Environments, variables, volumes, domains, deployments; CI runs, workflow dispatches, repository secrets, branch pushes (always after my yes). |
| Telegram Bot API, through the browser and the command line | The monitoring bot (`@permitflow_space_monitor_bot`) was created with @BotFather in Telegram Web driven by Claude in Chrome, on the owner's signed-in account; the token was copied from the page into a file and set on Railway from the command line without appearing in the chat; the command menu and the bot description were registered through the Bot API; test messages and the first digests were checked in the same browser tab. |
| ChatGPT (image generation) | The four architecture views for the technical deck (19 Sep) and their two updates for the observability layer (20 Sep): edit prompts written by the assistant against the code, each result rated against the original before use, one re-prompt each (a reversed arrow; four dropped variable lists). |
| LangSmith | Tracing of the product's verifier (off unless a key is set, inputs hidden by default) and the golden set as a dataset with one experiment per evaluation run, so a prompt change has a pass-rate history. Not used to write code. |
| OpenAI `gpt-4.1-mini` | Inside the product only: the advisory document verifier behind a provider interface, strict JSON schema on the wire, a deterministic mock in tests and the CI gate, the real model in the nightly evaluation. Not used to write code. |

## 2. How the work was organised (the workflow the AI worked inside)

The tools did not set the process; the process was fixed first and the tools were driven through it. Who did what, in one paragraph: every story was my decision, made in the chat and recorded on the Notion board; the AI drafted the story text and, on my instruction, created the board entry and moved its status. Nearly all of the application code and most of the documents were generated by the AI from those instructions; I made the decisions recorded in this file and in `SCOPE.md`, wrote the prompts, read every diff before a commit, and made small edits by hand in Cursor. No review agent edited code: each review returned findings, and every fix was made in the main session after I had read the finding.

1. **Understand the problem** (17 Sep): read the brief twice, wrote `docs/01-discovery/PROBLEM.md` (personas, pain points, success criteria) and extracted every requirement into `docs/02-requirements/REQUIREMENTS.md` with ids (FR, NFR, SEC, AI, AUD, UX, REL) and the use cases into `docs/02-requirements/USE_CASES.md`.
2. **Define the scope** (17 Sep): `SCOPE.md` with MUST, SHOULD, COULD and DEFERRED tables, the assumptions where the brief is ambiguous, and the stack in five sentences. Use case 3 deferred with its statuses kept.
3. **Solutioning** (17 Sep): `docs/03-architecture/SOLUTIONING.md` (options per problem), the domain model, the state machine as a table, the first nine ADRs, the threat model, the test strategy. The AI wrote drafts against the requirement ids; I decided each option.
4. **Design phase** (17 to 18 Sep): design direction, tokens, screen and component inventories, UI states, a 23-artboard clickable prototype, two independent design-critique passes.
5. **Agile delivery** in three one-day sprints with a Notion board mirrored 1:1 by `docs/05-planning/USER_STORIES.md` (US-000 to US-073), a sprint plan with goals, cut order and a close ritual (`docs/05-planning/SPRINTS.md`), and a definition of done per story (tests, docs, Notion, env vars, migrations, ADRs, scope). Sprint 1 "An operator can submit", Sprint 2 "The loop closes, twice", Sprint 3 "Ship it honestly". Each story on its own branch from `dev`, merged `--no-ff`; `main` only at releases (`docs/09-operations/BRANCHING.md`).
6. **Implementation** story by story, with the per-story checklist in `CLAUDE.md` enforced every time: tests per layer, browser check at three widths, docs updated in the same change, Notion moved.
7. **CI/CD and deployment**: CI with backend, frontend, end-to-end (including the accessibility gate), secret scan, dependency audit and image jobs; a six-stage AI gate called from CI (model approval, contracts, golden set, adversarial, fairness, verdict); a separate live evaluation against the real model; images built once to GHCR; two Railway environments on the owner's domain; production behind a human approval; health gates after every rollout (ADR-011).
8. **Reviews**: three edge-case reviews after Sprint 2, a layout audit, three bug hunts, three persona run-throughs, an independent review against the brief, a final bug hunt, and a legal, privacy and accessibility review on the last day; every finding fixed with a test or recorded as a decision (`docs/11-reviews/ISSUES_AND_MITIGATIONS.md`, `docs/11-reviews/LEGAL_AND_ACCESSIBILITY_REVIEW.md`).
9. **Documentation and release**: README, this file, AI assurance, production readiness, UAT, traceability and final review; then the v0.3.0 release.
10. **Debrief material** (after the release, second session): pitch deck, launch video and narrated walkthrough, planned against the same documents and reviewed by personas before being built, so the story told on stage is the one the repository supports.
11. **Cold reviews and the observability layer** (20 Sep): two fresh sessions were briefed as an independent reviewer with no memory of the project, one on the repository and one on the decks; their findings were taken as a list (the queue reading the operator's working copy, the README's length, the stale counts and the branding in the generated comic strips, and "no observability story"); the last item became US-077, built the same day: a metrics endpoint, Prometheus, Grafana, alert rules, an hourly digest and a command bot on Telegram, locally and on Railway, with the documents, diagrams and decks updated in the same story.

## 3. The context the AI was given

Two instruction files were loaded into every session (the project one is checked in at the repository root as `CLAUDE.md`, so a reviewer can read exactly what the AI was told):

- A global `~/.claude/CLAUDE.md` with engineering standards: no `any`, typed props and state, every `useEffect` cleaned up, no unrequested refactors, propose before changes over 20 lines or several files, build and test before every commit, conventional commit subjects, never push without asking, never mention AI tools in commits.
- The repository `CLAUDE.md` (checked in) with the project rules: the three personas and that operators never receive internal status codes; the modular monolith layering (`api → services → domain / repositories → models`, `domain/` pure Python); every status change through `domain/workflow.py`; AI never mutates application state; revisions immutable and audit rows written in the same transaction; upload allowlist, size limit and magic-byte check; the error envelope; the documents that are sources of truth (`SCOPE.md`, `REQUIREMENTS.md`, `STATE_MACHINE.md`, `DOMAIN_MODEL.md`, design docs); the Notion board ids; the per-story checklist (tests, docs, Notion, env vars, migrations, ADRs, scope); the sprint close ritual; UI rules (no em dashes, no emoji or gradients, red only for the brand mark, one primary action per screen, label plus dot for every status, layouts verified at 390, 1024 and 1280).

The brief lists five kinds of context an engineer should give the AI. Each was in use here, one example per kind, all quoted from section 4 or the standing files:

| Context type | Example given to the AI |
|---|---|
| Data model or schema | "Here is `ApplicationStatus` (14 states) and the transition table in `domain/workflow.py` (source, target, actor, guard, label); write the transition service so that every status change goes through it, and a test that iterates every (state, target, actor) combination." |
| Coding standards | "No `any`, typed props and state, every `useEffect` cleaned up, `mypy --strict` and `tsc --strict` clean before a commit, conventional commit subjects." (`~/.claude/CLAUDE.md`) |
| System constraints | "Three roles; operators never receive internal status codes; the AI never mutates application state; revisions are immutable and audit rows are written in the same transaction." (repository `CLAUDE.md`) |
| Acceptance criteria | "Operators edit only the flagged sections and documents; editability is derived from open, released feedback; a section that was not flagged returns 403; items whose target changed become `addressed` automatically." |
| Output format | "Modular monolith: `api` calls `services`, `services` call `domain` and `repositories`; `domain/` is pure Python; return the error envelope `{ error: { code, message, details? } }` everywhere." |

Before any code, the solutioning documents were written and reviewed (requirements with ids, use cases, domain model, state machine, ADRs, threat model, test strategy), then a design system and a clickable prototype. The code phase started from those documents, so every prompt could point at an id (FR-024, SEC-005, T5) instead of re-describing the requirement.

## 4. Prompts and instructions

Two kinds. The standing instructions lived in the two `CLAUDE.md` files and applied to every session; the session prompts were typed during the work. Both are given in the form they were meant: the wording is edited for readability (spoken prompts were dictated and carried filler), the content, order and scope of each request are unchanged, and each is followed by what it produced. Grouped by the decision they carry rather than by date.

### Architecture and domain rules (standing instructions)

- "Three roles, never reduce to two: operator, officer, admin as read-only oversight. Use case 3 is deferred, but its three statuses must exist in the state machine and be tested, because the brief lists them."
- "All status changes go through `domain/workflow.py`. Write the transition table as data (source, target, actor, guard, label) and a test that iterates every (state, target, actor) combination and asserts allowed, forbidden or invalid." (588 parametrised cases; the officer's actions and their disabled reasons are derived from the same table)
- "Operators edit only the flagged sections and documents. Editability is derived from open, released feedback; a section that was not flagged returns 403. A resubmission is a new immutable revision, and items whose target changed become `addressed` automatically."
- "Feedback is a draft until the officer requests resubmission; then it is released and frozen. Officers resolve only items the operator has seen. Withdraw and resolve get a 10-second undo, and the undo is audited."
- "The AI never mutates application state, feedback or status. It writes a run row; a person decides." (the advisory principle every later AI decision was held to)
- "Modular monolith: `api` calls `services`, `services` call `domain` and `repositories`; `domain/` is pure Python with no framework or I/O imports." (enforced by a layering test)

### Scope and product decisions (session prompts)

- "Don't create a new user story for this; you are increasing the scope." (a proposed "accept AI finding" story, dropped; the approval path was explained in the UI instead)
- "What if the operator could download the licence: render a certificate on approval, let the officer preview it before deciding, and let the operator download it afterwards." (US-051, marked beyond the brief in `SCOPE.md`, kept on its branch until I had reviewed the rendered PDF)
- "Keep the feature and fix its label instead of reverting it." (after the AI offered to remove a feature to tighten scope)
- "Reject this application, then add a warning at approval rather than a hard block." (the Approve dialog warns about unresolved checks and stays enabled, so the officer decides)
- "Add Return to review and fix the verifier prompt; leave the other three scope items." (two of five proposed items taken; the rest recorded, not built)
- "One naming convention per environment on the domain: `permitflow.space` and `api.` for production, `dev.` and `api.dev.` for development; keep the platform hosts as fallbacks." (US-052)

### Quality, reviews and evidence (session prompts)

- "Test each workflow separately, one Playwright spec per workflow, each ending on the audit trail." (US-042)
- "Deploy a review agent for orientation and layouts at every width." (`docs/11-reviews/LAYOUT_AUDIT.md`)
- "Deploy three bug-hunt agents with separate briefs: backend rules, frontend interaction, the seams between them. Reproduce before reporting." (US-050, 39 findings, 36 fixed, 3 kept as decisions)
- "Do a run-through in the browser with the real demo documents, as each persona, and take notes on every step." (three run-throughs; the first found two defects the tests had missed, the third was the Sprint 3 acceptance on the deployed environment)
- "Review the application, its output and the standards we follow against the brief, independently and without allowances, and report what can be improved." (its report was not kept as a file; it scored documentation 5/10, and its findings became the README sections, `docs/11-reviews/PRODUCTION_READINESS_REVIEW.md` and this file)
- "Check Notion, every user story and every document against each other and against the code; everything must be consistent." (the consistency pass before the release: dates against `git log`, API table against the routers, state table against `workflow.py`)
- "Record every issue found and its mitigation." (`docs/11-reviews/ISSUES_AND_MITIGATIONS.md`)
- "Raise unit and integration coverage of the business logic to about 80 percent, measured with every source file counted, testing behaviour rather than buttons, and enforce it in CI." (US-053: frontend 47 to 80.6 percent statements, backend 96 percent, thresholds in both jobs)

### Delivery and operations (session prompts)

- "Keep Notion in step with the work, and define the merging and branching strategy." (`docs/09-operations/BRANCHING.md`: story per branch from `dev`, `--no-ff` merges, `main` only at releases)
- "Take the cut: build the images once in CI and push them to GHCR; Railway pulls." (ADR-011)
- "Production must be approved by a person." (GitHub environment with a required reviewer, `main` only, health gates after the rollout)
- "Make sure every change is reflected in the architecture documents and the diagrams too." (docs in the same change, every time)

### AI assurance (session prompts)

- "If someone asks how we know the verifier's output is legitimate, the answer must be a pipeline, not a paragraph: a separate evaluation against the real model, tracing, and the AI checks as a gate of their own." (US-054 nightly and on-change live evaluation, blocking at 14 of 14; US-055 LangSmith tracing with inputs hidden by default; US-056 the six-stage AI gate called from CI)
- "Add a fairness check: the same document with the applicant's and the business's names swapped across Singapore's communities must get the same verdict." (21 name-swapped runs, mock and live, both blocking; the first live run's two differences were a harness fault and are recorded as such in `docs/07-ai/AI_ASSURANCE.md`)

### Security hardening (session prompt, the whole checklist)

A second checklist, given whole for the same reason: "I do not want the platform to be compromised. Keep the API keys out of the code and check every environment variable. Protect the admin routes. Confirm that authentication is sound and that each user can reach only what their role allows. Validate and sanitise every form input and guard against XSS. Rate-limit the API and harden every endpoint. Review the CORS settings, add security headers and make sure debug mode is off in production. Update the dependencies and remove any that are unused. Check for exposed files. Secure the database and confirm that passwords are hashed properly. Scan the git history for leaked secrets and run a full security audit. Work carefully. Then assume an attacker who hits the backend ten thousand times a minute until the login page fails, floods sign-up until the database fills with junk, calls the most expensive endpoint in a loop until the API bill is enormous, and scrapes the data without being noticed. Against that, rate-limit every endpoint, sign-in and sign-up first."

The audit before any code found most of the list already in place (Argon2, JWT re-checked per request, ownership as 404, allowlisted CORS, docs off in production, gitleaks clean over the whole history) and two real gaps: rate limiting covered failed sign-ins only, and nothing capped the model-calling path. It also found that `pip-audit` had been failing silently in CI since it was added. The work: a per-client limiter on every request and on sign-in attempts, database quotas on drafts and on verification runs per applicant and per platform, CSP and the other headers on both tiers (the bundle driven under the exact policy to prove nothing broke), the audit job made correct and blocking with `bandit` added, nine tests, and `docs/06-security/SECURITY_REVIEW.md` with each attack's before, after and test (US-058).

### Debrief material (session prompts, second session)

- "Study the repository first and do not build anything yet; plan a pitch deck for the debrief, a short launch video and a narrated walkthrough." (plans kept in a git-ignored workshop folder)
- "Review the plans as a short-form creator, a motion designer, a product-video editor and the intended audience, and iterate until each scores nine or above." (fourteen revisions of the launch video plan, four of the walkthrough)
- "Footage from the real product with the live provider; voice on both films; instrumental bed; the problem first, then the launch; button labels verbatim; nothing the operator cannot see." (the product-truth rules the build must obey)

- "Check the codebase, how it is structured and deployed, and write one prompt per diagram (solution architecture, logical, physical, deployment, CI/CD) that I can paste into an image generator with my own template; then rate what comes back as correct or not." The prompts named every component from the workflows, Dockerfiles and operations documents and forbade anything else; each carried an acceptance list (for the physical view: exactly two environments, PostgreSQL 18, the browser calls the API directly, LangSmith from development only, no load balancer, CDN, Redis or second region). Three pictures were accepted after one revision each, one prompt was skipped, and the physical view was dropped after three attempts kept mis-routing arrows. The accepted pictures are in `docs/03-architecture/diagrams/views/`.
- "Build the technical deck in the same design language as the pitch deck, more technical: scope, how it was scoped, the main ADRs, not all twelve." The four chosen were the ones a line of the brief can check (the status table, operators never see the approval stage, the AI advises, nothing lost between rounds); the shape decisions went on the architecture slide. Facts on the slides were pulled from the repository during the build and three counts in the first draft were corrected against it.

### Cold reviews and the observability layer (session prompts, 20 Sep)

- "A reviewer will not read the whole repository; they will clone it and ask an assistant to rate it. Take that persona, rate the repository out of ten against the brief's five areas, and point out the fixes." (a fresh session, cold context, read-only; 8.5 of 10, three fixes, all three taken)
- "Review both PowerPoints once more with the same reviewer persona, then fix whatever it finds and rebuild the decks." (the comic strips removed for real government identity in generated panels, five counts and one claim corrected, a code slide and a "where the AI was wrong" slide added)
- "Can we have an observability layer, Prometheus or Grafana? Propose it first, then build it, deploy it on our domain, keep development and production apart, make the dashboard read like a big screen with a title, add the OpenAI cost from the tokens, and check the model's price on the internet." (US-077; the price checked on the OpenAI pricing page and written next to every use of it)
- "Update every diagram and deck that the layer touches: copy each image into ChatGPT with an added prompt, rate the result, re-prompt if it is wrong, and replace the images in the decks." (two diagrams regenerated, two re-prompts, CI/CD and branching left unchanged on purpose)
- "Give the documentation an observability folder of its own, numbered 13, and renumber the debrief to 14; make sure every number and every mention is in step, and verify twice." (the folder, the renumbering across the repository, a mechanical pass and a fresh read-only reviewer)
- "Can I get Telegram alerts, and an hourly digest? Make the message look formatted, show development and production separately, and give me commands so I can ask what is happening." (Grafana-managed rules with a Telegram contact point, an hourly digest per environment, a standard-library command bot; the bot created in Telegram Web through the browser)
- "Once it is live, count only development and production, not local." (the local Grafana provisions the removal of its alerting when the Telegram values are empty; the local bot is stopped)

### v0.4.0 on the development branch: the site visit appointment (session prompts, 20 Sep, evening)

Production was frozen for the assessment; everything below landed on `dev` only. Prompts edited for readability, in order:

- "Why is the date not selected? After the resubmission the officer goes straight to the site visit and then to approval, but nowhere does anyone pick a date. Where is that workflow?" (there was none: the brief has the status only; the answer was the appointment, US-084, designed on three artboards and approved before any code)
- "The officer proposes a date; the operator accepts or proposes another one; the officer accepts, rejects or keeps the earlier date; both sides are notified." (the product rule; the AI added the working-day notice periods, the three-working-day silence rule and the reason on every counter)
- "How many loops of rescheduling can happen? Is there a limit?" (there was none; capped at six proposals per visit with the closing moves always open, one constant, tested at the boundary)
- "Run two or three Sonnet reviewers in parallel against the new build and give me the bugs." (three read-only reviewers: backend, frontend, security and document drift; one real bug found and fixed, keeping the original date after a reschedule reverted to a stale round; the officer's name was masked in the operator's rounds; the phone layout of the officer's forms was fixed)
- "Test it yourself in the browser, with the real flow, and rate how easy each function is to use. Then check the layout at phone, tablet and laptop sizes, and what happens to the buttons, text and columns when the page is zoomed in or out on the same laptop." (a Claude in Chrome run on the local stack found five copy and layout faults that the tests had not; a sweep at 390, 768, 1024, 1280 and 1512 pixels and at 80, 125 and 150 percent zoom showed no overflow and one consistent rule: below 1024 pixels the panels stack above the submission, at 1024 and above they form the right rail)
- "The scrolling area in the rail looks amateurish. Take screenshots and judge it yourself." (a native scrollbar track beside the cards; replaced by a thin thumb that appears only on hover, in its own gutter)
- "What does 'A working day, from 22 Sep 2026' mean?" (unclear help text; rewritten as "Monday to Friday, 22 Sep 2026 or later (two working days' notice)")
- "Keep the number of tests proper and the total coverage above 80 percent; run some extra checks in other time zones; keep the English simple and in the same voice as the rest of the site." (787 backend cases, 181 frontend tests, 13 Playwright runs, 204 edge checks; backend 95 percent and frontend 82 percent statements; the suites run again under three time zones; every new string read once more against the site's voice)

### v0.4.0, the rest: use case 3, sessions, storage, the admin panel (session prompts, 21 Sep, overnight)

The owner set the frame in one message and left for the night: "make sure all the use cases are done for 0.4.0, add the use cases you judge should be added, loop, keep the UI clean and polished, no further bugs, use Sonnet agents with high thinking for the reviews; I will be reachable on my phone", then "do everything by morning; any step you take, normal precautions, nothing destructive". Everything below landed on `dev` only, story branch by story branch, merged with `--no-ff`; nothing was pushed or deployed. Prompts edited for readability, in order:

- "For the site visit checklist, can the officer add items himself? And could one problem need two findings on the same item? If that is too much, defer it, but log it in Notion and the documents." (US-092: findings of the officer's own under Other findings, a second finding under a template item, both saved with server keys; the draft also sent when the tab goes to the background, after "what if the iPad is closed without saving, autosave every 30 seconds, a browser alert on refresh")
- "Unselecting: if I press Satisfactory again, does it clear? Add that." (pressing the selected result clears it back to Not assessed)
- A screenshot: "Post-Site Clarification Resubmitted overlaps the button at the iPad width; take care of this." (the queue badge wraps; layout audit H4 closed)
- "The same account: the officer opens it on the iPad; if someone logs in on his account elsewhere he should not be able to; he saves, comes back to the laptop, and it starts from there. Think about all the functional and non-functional requirements for this." (US-093: a server-side session per sign-in, the token carrying its id, a second sign-in refused and named, take-over from the sign-in page, revocation on the next request, a 60-minute idle limit, continuity through the server-side draft; the AI wrote the story with NFR-019 and T27 before the code, and every test helper had to sign in with `take_over`)
- "Think about the things you are adding; make sure you add them in Notion too." (every added story on the board in the same turn, with its Version)
- No prompt for the rest: the loop continued through US-085 (storage budget, photos stripped of camera data), the admin epic (US-070, US-072, US-073, with account creation from the page because the owner had asked for it earlier), US-089 (the dashboard row), US-088 (the accessibility gate, which found two real defects), US-087 (poor connection), US-083 (phase fields), US-086 (load budgets, which found the overview at 2.8 s), the sprint closes and this document.

What the AI got wrong in this stretch, briefly: it ran the UAT edge script and the Playwright suite at the same time, and one signed the other out (the new session rule); it read a Python `\x89PNG` fixture as a valid image in three tests until the new image pipeline refused it; it put new dashboard panels on ids the cost row already used; it wrote an ISO cursor with a `+` that a query string turns into a space. Each was caught by a test or a run, not by a reader.

### The morning after: the two-device run, the stability review, the release candidate (session prompts, 21 Sep)

Prompts edited into plain sentences, in order; the work each produced is in `CHANGELOG.md` ("UAT run 4", "Stability review") and in `docs/10-uat/UAT_PLAN.md` (U16, run 4):

- "Some stories are still In progress in Notion; which are they and why?" (US-080 and US-082 waited for a deploy of `dev`; US-081 for the release; US-091 parked.)
- "Run two Claude in Chrome tests with different scenarios, note every step for me, and check that everything in use case 3 is done. Use different tabs or windows to try the iPad timing: the officer opens the checklist on an iPad, does not press Save, it autosaves; he comes back to the MacBook and expects it to be there. Try the concurrency cases." (Run 4: five tabs as two devices, two officers, the operator and the administrator; three stale-page findings fixed the same hour.)
- "Everything is cleared in the new tab; I am back at Mark site visit scheduled. Why is it not autosaved? Solve this and try it in different ways." (The tab had opened the second seeded case; nothing was lost. The finding was recorded in the UAT plan and the remaining concurrency angles were tested, which produced the three fixes.)
- "Explain how the iPad switch is handled, in simple words and in technical words, and put every learning and the new implementations in the debrief material." (Part 2b of the private preparation notes.)
- "Recheck all the v0.4.0 code with subagents, three Opus and two Sonnet; check that everything is stable; ping the backends; smoke-test every endpoint; fix any bugs, edge cases and gaps you find; keep the documents consistent; then ping me." (Five reviews, a route-coverage smoke test, the edge checks, 30 findings fixed with tests, readiness row 26.)
- "Merge everything to `dev`, make sure CI passes, tag it well, do not touch `main`; check the live development URL; update Notion and the release notes; every Markdown file live and correct; run two or three subagents in parallel; keep the code base tidy." (This section, the release candidate tag on `dev`, the tidy and consistency audits.)
- "The policy pages have too much margin; spread them, and make them react to the side rail opening." (The policy opens inside the app shell when signed in, with a section list beside the text.)
- "`AI_USAGE.md` should carry the prompts I used, refined, not verbatim, no colloquial language." (This list.)

### Legal, privacy and accessibility (session prompt, the whole checklist)

Given whole because the AI's first move was to audit the code and cut the list down before building anything: "I do not want the site to expose me legally. Add a privacy policy, terms and conditions and a cookie policy; check whether cookie consent is needed at all; add a refund policy and consent on forms if they apply. Collect only the data the product needs. Check for analytics tracking and third-party embeds. Make the site accessible: alt text, colour contrast, keyboard-friendly forms, clear button labels. Remove any fake reviews or unsupported claims. Add the business details and check the copyright of every image. Check the applicable local laws and flag any other risk. Work carefully. Create a user story for this and record the outcome in a document a reviewer can read."

The audit found no cookies, no analytics, no embeds, no images and no reviews, so five items became "not applicable, and here is why". The work that remained: policies written from the code with PDPA framing, two demonstration-only notices, self-hosting the fonts (the browser's one third-party request), an axe-core gate over 23 screen states that found and fixed four landmark, skip-link and contrast defects, and `docs/11-reviews/LEGAL_AND_ACCESSIBILITY_REVIEW.md` (US-057). Two decisions were mine when asked: operator named with the repository link and no email; fonts self-hosted rather than disclosed.

### Final check before submission (session prompts, the two requests)

The last session ran overnight, unattended, on the eve of submission. Its two prompts, edited for readability: "Do one last review of the whole codebase and every document, top to bottom; I submit in the morning. Fix what you find and verify everything again. Do not push; work on dev locally. Run UAT cases and make sure no edge case is missed." Then, after the first status line: "You may use permitflow.space or dev.permitflow.space for testing. Follow our coding practices, the branching strategy and the user-story process. Do not merge anything into dev. Record these prompts in the AI usage document."

What the AI did with it, in order: ran every suite from the cold clone (two vitest failures turned out to be a Node 22 against Node 24 difference in how a `Response` wraps a `Blob`; the test was made version-independent); spawned three read-only reviewers with separate briefs (backend correctness and authorization, frontend behaviour and the copy rules, every document against the code) and verified each finding in the code before acting; wrote an API-level edge-case driver of 166 checks covering every refusal path it could think of, ran it against the local stack, and kept it as `backend/scripts/uat_edges.py` with the record in `docs/10-uat/UAT_PLAN.md`; fixed what the reviews and the run established (listed under "Final check" in `CHANGELOG.md`, US-075); corrected the documents where they described something the code does not do; re-ran everything; and, on the second prompt, moved the work onto `feat/us-075-final-check` from `dev`, added the story here and on the board, and left the merge to the owner. Five of the driver's first failures were the driver's own wrong expectations (the operator labels "Pending Site Visit" and "Pre-Site Resubmitted" are the brief's own wording, not officer-only; a reopened item is a draft again by design) and were corrected in the driver, not the product. One Playwright failure during the re-runs was the local operator account reaching its 60-checks-a-day quota after the repeated runs, which the README already warns about; the run was repeated with the quotas off.

### UAT run 5, its fixes, and the Cloudflare security audit (session prompts, 24 Sep)

Prompts edited into plain sentences, in order; the work is in `CHANGELOG.md` ("UAT run 5 and its fixes") and `docs/10-uat/UAT_PLAN.md` (runs 5 and 6):

- "Run a Claude in Chrome test of use case 3 and every edge case, end to end, with separate tabs to emulate an iPad, and follow every case in the UAT plan. Use dev.permitflow.space. Check page refreshes and how autosave behaves on flaky Wi-Fi, how multiple logins are mitigated, and the iPad-to-laptop switch. Note every bug you meet." (Run 5: 20 findings, four of them bugs.)
- "Fix everything on a separate branch and make sure it is correct; nothing should remain; every UAT case must pass; test the failed cases again afterwards; run strong smoke tests on every endpoint; delete the unnecessary accounts created while testing." (Branch `fix/uc3-uat-findings`; four product decisions asked and answered: block the visit until its day, reply-by the day before, keep the checklist open during a move request, reuse components for earlier visits; run 6 re-tested every failed case; smoke test extended to 122 checks; throwaway local accounts purged.)
- "Push to GitHub safely; open the pull request; set the visit-day guard on the development environment; merge and check the live site." (PR #12, CI green, merged into `dev`, deployed, live check 10 of 10; `SITE_VISIT_DAY_GUARD=false` on the development backend only.)
- "Install the Cloudflare security-audit skill and run it on PermitFlow, on a new branch, and record the skill here." (This entry; profile and budget chosen by me when asked: standard, 15 agents.)

## 5. How output was reviewed, validated and corrected

- Every story ran the full backend suite on a real PostgreSQL (`permitflow_test`, migrations from scratch), vitest, and Playwright where a UI path changed; `ruff`, `mypy --strict` and `tsc --strict` had to be clean before a commit. CI repeats all of it with coverage thresholds, plus the six-stage AI gate, the accessibility gate, gitleaks and dependency audits; the live model is evaluated nightly and on every change to the AI module.
- Every screen was checked in the browser at 390, 1024 and 1280 before its story moved to Done; two full persona run-throughs with the demo PDFs were done on the last day and found two defects the tests had not (the licence download hidden when the approval carried no note; a misleading feedback heading). Both were fixed with tests.
- Prompt changes to the product's verifier had to pass the 14-case harness on the mock in CI and against OpenAI; the harness rejected two of the three wordings tried on the last day (one left the false finding in place, one turned clean documents into `issues_found`, 11 of 14). Only the third shipped. The 21-run fairness check was added after that and passes on the live model.
- Subagent reviews were treated as claims, not facts: each finding had to come with a reproduction and was re-verified before a fix; three findings were kept as product choices and documented rather than "fixed".
- The observability layer was verified three ways: the counters read back through the endpoint after a scripted round of applications; a fresh read-only reviewer that compared every metric family, label, alert threshold, dashboard row, price and count in the documents with the code (six mismatches found, all fixed); and the deployed chain watched end to end in Telegram (the digest, a command reply, an alert firing and, once the endpoint shipped, resolving).
- Docs were checked against code at the end of each sprint and once more on the last day (API table against the routers, state machine table against `workflow.py`, test counts, dates against `git log`).
- Git hygiene: story per branch, `--no-ff` merges into `dev`, `main` only at releases; the AI proposed commit messages, I read every diff before the commit; pushes only on my explicit yes.

## 6. Where the AI was unhelpful, wrong, or produced work I discarded

What I would not defend line by line: the reportlab layout code for the licence certificate (`backend/app/infra/pdf/licence.py`, three iterations, correct by rendered output rather than by reading), the Tailwind class strings across widths in the larger pages (`CasePage.tsx`, `FormPage.tsx`, verified by browser checks and the axe gate, not by inspection), and the fairness harness (`backend/evals/fairness.py`), whose first run blamed the model for a fault in its own substitutions. Everything else I can explain from the file.

- First live run of the verifier: the model invented enum values (`status: rejected`, `severity: error`) and reported an expired certificate as valid because it had no date. Fixed by pinning the vocabulary in the wire schema and passing today's date in the prompt; recorded in `docs/07-ai/AI_VERIFICATION_DESIGN.md`.
- The AI proposed a new user story for "accepting an AI finding" and, separately, a full red landing band; both were rejected (scope creep; too heavy) and are recorded in the story notes.
- The AI offered an approval-stage option ("add feedback and request resubmission") without checking the state machine; it was impossible from Pending Approval. That mistake led to the Return to review transition, a real gap.
- The first certificate layout overflowed on long addresses, hid the brand mark behind a plain square, and a later "sliding" signature strip collided with the footer; three iterations, each checked as a rendered PNG, before it shipped.
- A subagent's scratch test file was swept into a commit by `git add -A` while it was still running; removed and recommitted, and staging became explicit from then on.
- Notion notes were written with the wrong day (20 Sep) for events that happened on 18 and 19 Sep; corrected in the consistency pass. Two remote CI runs failed on `ruff` line length the AI had not run locally; the pre-commit routine was tightened.
- Licence dates were first computed in UTC (a day off in Singapore); fixed to `Asia/Singapore`.
- The verifier flagged the demo documents' "fictional document" footer as a possible prompt injection; it took three prompt wordings and the harness to fix without breaking the clean cases.
- The AI's own strict review on the last day scored Documentation 5/10 because the two README sections the brief names were still missing while nice-to-have features had been built; the review's report was not kept as a file, its findings became the README sections, `docs/11-reviews/PRODUCTION_READINESS_REVIEW.md` and this document.
- The first fairness run reported 2 of 21 name-swapped cases as differing, and the AI's first reading was "the model is sensitive to names". It was the harness: the form still carried the baseline applicant's email and the document its upper-case director line, so the model was right that form and document disagreed. Two more swaps and it was 21 of 21, twice. Recorded in `docs/07-ai/AI_ASSURANCE.md` because a fairness check that hides that would be measuring the wrong thing.
- The observability layer, 20 Sep: the first Railway deploys of Prometheus and Grafana failed on volume permissions (the images run as a non-root user, Railway mounts volumes as root; `RAILWAY_RUN_UID=0`); a Railway `redeploy` re-ran the previous snapshot and so kept an old start command, which cost one round; Grafana refused a Telegram contact point whose chat id its own variable expansion had turned into a number (the values now go in with `sed`, inside quotes); a dashboard query used `$1xx` where PromQL wanted `${1}xx`, so one panel showed a single unlabelled series until it was read; an anchor tag in the Telegram message was not tappable in the owner's client, so the messages carry bare URLs; and the first local screenshots showed "No data" for the checks because the counters had been created and incremented before Prometheus's first scrape, which `increase()` cannot see, an artefact of the test order rather than a fault, recorded so the next reader does not chase it.
- Two configuration slips on the last day: a vitest coverage option that the installed version had removed (caught by `tsc -b` in the build, not by the test run), and a repository secret set from the wrong `.env` file, so the first live evaluation ran with an empty key and refused, as designed. Both fixed within the hour.

## Appendix: the second session (decks and videos)

The pitch deck and two videos, started 19 Sep after the code was frozen. Skills used: `brag` (the launch video workflow: plan, storyboard, audio rules; about 30 seconds, narrated, captioned, beat-snapped cuts) and the official HyperFrames skill set installed with `npx hyperframes skills` (`hyperframes-core`, `-animation`, `-keyframes`, `-cli`, `-audio`, `media-use`, `general-video`, `slideshow`) for the composition, punch-ins, captions, music and rendering; `demo-video` for the Playwright footage recipe (headless 1920 x 1080, drawn cursor, one browser per scene, waits detected rather than timed); `claude-video-generator` for the narrated-walkthrough rules (narration length sets every hold, honesty checks, a leak sweep of the recordings for emails and ids). Voice: Kokoro `af_heart` through `npx hyperframes tts` (nine takes). Music: the HeyGen catalogue through the `media-use` helpers with the owner's own sign-in (`npx hyperframes auth login`), no generated music; a music-supervisor subagent ranked eight catalogue tracks by measurement and two were chosen. The session read the repository only, with the code as the source of truth over the design documents (tokens and fonts from `index.css` and `fonts.css`, operator labels from `operator_view.py` and `VerificationBlock.tsx`, feedback wording from `feedback_templates.py`, licence format from `domain/licence.py`, form values from `evals/cases.json`, selectors from `e2e/helpers.ts`). Plans were reviewed by four persona subagents (short-form creator, motion designer, product-video editor, audience) through fourteen revisions before any build. How the footage is made: a scratch Playwright script (`notes/pitch/record/record-launch.mjs`; the `notes/` folder is git-ignored, so these files are not in the repository) drives the deployed development environment, not production, with a fresh Chromium per scene at 1920 x 1080, a real sign-in as each persona, the demo PDFs, the live provider, a drawn cursor with distance-scaled glides, and writes one `.webm` plus an events file (timestamps of every click and state change) per scene for trimming, plus contact-sheet frames for review; six scenes were recorded first (operator check, officer case, operator fix, officer rail, route to approval, approve). Narration is written before filming for everything that does not depend on the model's answer and from the actual take for the lines that do; voice WAVs set every hold (a clip never advances mid-sentence). Assembly, captions, the bed and SFX go into a HyperFrames composition (`notes/pitch/launch/composition`), checked with `hyperframes check` and `snapshot` before a 60 fps render. Working files live in the git-ignored `notes/pitch/`. The launch video was finished and approved by the owner on 19 Sep (18:50 SGT): 70.7 seconds at 1920 x 1080, 60 fps, fifteen narrated lines with captions, footage from a fourth take on the development environment recorded on a 3840 x 2160 canvas so punch-ins stay sharp, 31 live checks spent against the applicant's daily quota of 60, the poster baked as frame 0, share copy for two platforms. The deck exists in two forms from one content file, an HTML slideshow and a PowerPoint with click builds, both through the reviewer loop. The narrated walkthrough was finished and approved the same evening: 4 minutes 36 seconds at 1920 x 1080, 60 fps, subtitles and a poster frame, recorded on the development environment against its own application, with the narration script kept beside it; a technical video (five minutes forty seconds: roles and the 403s, the status table, the test counts, the six-stage AI gate, the fairness sweep, scope, then a chapter on how it was built: the terminal, the dictation bar, the editor and the Notion board reconstructed as HTML windows in the product's tokens with text from this file and the git log, real GitHub recordings of the checked-in `CLAUDE.md` and of the three skill repositories, and the film's own `build.py` as the last beat, and an observability chapter added on 20 Sep: the three Grafana renders from `docs/13-observability`, the alert rules, and the Telegram bot's digest and commands reconstructed as a phone window with the numbers taken from the local renders and every environment shown up, the pre-release state masked on the owner's instruction) was approved on 20 Sep after three rounds of notes. Footage for all three was captured at 4K and delivered at 1080p; the music beds are catalogue tracks ducked under the voice; production was never touched. A second, technical deck (twenty-three slides: scope, assumptions, the board, architecture, four ADRs, branching, deployment, CI, readiness evidence, four slides on AI usage, one of them the gist of the checked-in `CLAUDE.md` and one the list of what the AI got wrong, the workflow table as code, the observability layer with its dashboard and its Telegram messages, next steps) was built the same evening in the first session with the pitch deck's generator; its four diagrams (solution architecture, branching, deployment, CI/CD) were generated by ChatGPT from prompts the assistant wrote against the workflows, Dockerfiles and operations documents, with an acceptance list per diagram; each picture was rated against the code before use, two versions were rejected for invented components (an Azure tenant, a CDN, a frontend that proxies the API) and the physical-architecture diagram was dropped after three attempts kept mis-routing arrows. On 20 Sep two comic strips (before and after, four panels each) were generated in ChatGPT from prompts the assistant wrote and placed in the pitch deck, then removed after a cold review of the decks pointed out that the generated panels reproduced real Singapore government identity (the lion crest, the Public Service wordmark, the GoBusiness brand) with a misspelling, and put back later that day at the owner's decision, with that risk noted here rather than hidden. The same review corrected five counts and one claim across the two decks; both were regenerated from the repository as it stood, the shared copies without presenter notes. Final outputs are in `docs/14-debrief/` (decks, handouts, the three videos); working files stay in the git-ignored workshop folder.
